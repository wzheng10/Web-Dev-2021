const { PI, square } = require('./math');
const colors = require("colors')")
const cats = require('./shelter')
// console.log(PI)

// console.log(square(9))
console.log("REQUIRED AN ENTIRE DIRECTORY!".green, cats)
